# Whether to enable the recursive opening feature
enableRecursiveOpening: true

# Maximum block distance for recursive search (1-32)
recursiveOpeningMaxBlocksDistance: 10

# Whether double doors are enabled
enableDoors: true

# Whether double fence gates are enabled
enableFenceGates: true

# Whether double trapdoors are enabled
enableTrapdoors: true

# Whether villagers trigger linked-door behavior (open/close partner doors when pathfinding)
enableVillagerLinkedDoors: true

# Global on/off switch for all DoubleDoors behavior on this server
serverWideEnabled: true

# Enable anonymous FastStats tracking
# Info we track is public usage data and can be viewed at: https://faststats.dev/project/double-doors-server/minecraft-plugin
enableAnonymousTracking: true

# FastStats token from your project settings (32 lowercase hex characters).
# Example: 0123456789abcdef0123456789abcdef
# Leave empty to use the built-in fallback token.
fastStatsToken: ""

# Enable additional anonymous telemetry like server location, countries, Java version, and system statistics.
enableExtendedAnonymousTracking: false

# Optional list of countries you want associated with this server, for example:
# trackingCountries:
#   - US
#   - DE
trackingCountries: []

# Optional human-readable server location label, such as EU, US-East, or Frankfurt.
trackingServerLocation: ""

# Active plugin language code.
# You can use full codes (e.g. en_US, de_DE) or aliases (e.g. en -> en_US).
# Built-in language files are loaded from subfolders under: lang/
# Custom language files can be added to: plugins/DoubleDoors/lang/
language: en_US

# Shared SQL storage configuration (Bukkit + proxy can use the same database).
sql:
  # If false, YAML files (players.yml, claims.yml) remain the active storage.
  enabled: false

  # Examples:
  # sqlite: jdbc:sqlite:plugins/DoubleDoors/doubledoors.db
  # mysql: jdbc:mysql://127.0.0.1:3306/doubledoors
  jdbcUrl: jdbc:sqlite:plugins/DoubleDoors/doubledoors.db

  # Leave empty for SQLite.
  username: ""
  password: ""

  # Run a one-time import from existing YAML files into SQL.
  migrateFromYaml: true

  # Bukkit considers proxy heartbeats newer than this as "proxy online".
  proxyHeartbeatMaxAgeSeconds: 180
